name: Android CI

on:
  push:
    branches: [ "master" ]
    tags: [ "v*.*" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: write
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v6
    
    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: gradle

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Decode keystore
      run: printf "%s" "${{ secrets.SIGNING_KEY }}" | base64 -d > ${{ runner.temp }}/keystore.jks

    - name: Generate keystore properties
      run: |
        cat <<EOF > keystore.properties
        storeFile=${{ runner.temp }}/keystore.jks
        storePassword=${{ secrets.KEY_STORE_PASSWORD }}
        keyAlias=${{ secrets.ALIAS }}
        keyPassword=${{ secrets.KEY_PASSWORD }}
        EOF

    - name: Build with Gradle
      run: ./gradlew build

    - name: Collect artifact
      id: artifact
      run: |
        outputs_dir="outputs"
        sudo mkdir -p "$outputs_dir"
        find app/build/outputs/apk -type f -name "*.apk" -exec cp {} "$outputs_dir" \;
        echo "apk_outputs=$outputs_dir" >> $GITHUB_OUTPUT
        
    - name: Use short hash
      id: hash
      run: echo "short_sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

    - name: Upload artifact
      uses: actions/upload-artifact@v7
      with:
        name: ${{ github.event.repository.name }}-${{ steps.hash.outputs.short_sha }}
        path: ${{ steps.artifact.outputs.apk_outputs }}

    - name: Rename APKs
      if: github.ref_type == 'tag'
      run: |
        project_name="${{ github.event.repository.name }}"
        tag="${{ github.ref_name }}"

        for file in ${{ steps.artifact.outputs.apk_outputs }}/*.apk; do
          filename=$(basename "$file")
          suffix=${filename#app-}
          suffix=${suffix%.apk}
          new_name="${project_name}-${tag}-${suffix}.apk"
          mv "$file" "${{ steps.artifact.outputs.apk_outputs }}/${new_name}"

    - name: Github release
      uses: softprops/action-gh-release@v2
      if: github.ref_type == 'tag'
      with:
        files: ${{ steps.artifact.outputs.apk_outputs }}/*.apk
        generate_release_notes: true
        
